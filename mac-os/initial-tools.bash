# This command installs the Xcode Command Line Tools on a Mac
# Xcode Command Line Tools includes compilers and other tools that are useful for creating command-line software
# It's also a prerequisite for some Homebrew installations
xcode-select --install
sudo gem install cocoapods